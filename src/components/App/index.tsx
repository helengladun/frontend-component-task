import React, {FC} from 'react';
// components
import UserForm from '../UserForm';
// styles
import styles from './styles.css';

const App: FC = () => (
  <div>
    <UserForm />
  </div>
);

export default App;
